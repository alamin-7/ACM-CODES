#include<bits/stdc++.h>

using namespace std;

int main()
{
    int i, j, x, a;

    long long int n;

    printf("Enter a decimal number:");

    scanf("%d", &n);

        a = bool (n &(1<<(j)));

        for(j =0; ;j++){

        if(a == 1){

            x++;
        }
    }

    printf("%d", x);
}
