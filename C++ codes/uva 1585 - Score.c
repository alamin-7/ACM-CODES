#include<stdio.h>
#include<string.h>

int main()
{
    char str[10000];
    int count, i, t, sum, count;

    scanf("%d", &t);
    for(i = 1; i <= t; i++)}{
            sum = 0;
            count = 1;

        scanf("%s", str);

        for(j = 0; j != 0; j++){
            if(str[j] == o){
                sum =
            }
        }
    }


}
